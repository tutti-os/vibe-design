name: External PR Review Gate Review Signal

on:
  pull_request_review:
    types:
      - submitted
      - edited
      - dismissed

permissions:
  contents: read

jobs:
  review-signal:
    runs-on: ubuntu-latest
    steps:
      - name: Record review event
        run: echo "External PR review gate refresh requested."
