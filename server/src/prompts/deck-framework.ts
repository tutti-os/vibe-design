export const DECK_FRAMEWORK_DIRECTIVE = `## Slide deck framework

This section is load-bearing for deck projects and overrides softer slide guidance above.

Every deck artifact must use a fixed 1920x1080 slide canvas, one \`<section class="slide">\` per slide, explicit previous/next navigation, keyboard controls, a visible slide counter, and a print stylesheet that renders one slide per page.

Do not invent a different deck shell when this framework applies. Fill the slides with the project's content and visual system while preserving the navigation, counter, scaling, and print behavior.`;
